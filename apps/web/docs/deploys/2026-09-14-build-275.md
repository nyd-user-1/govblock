# Build 275: the site is live again

Amplify job 275 succeeded on commit `0ce6b0b` at 02:22 EDT, 2026-09-14, after jobs 271 through 274 failed. Production answers on every route checked.

## Why the last four failed

All four compiled. Each died on Amplify's size cap: 307 MB of build output against a 220 MB limit. The weight was 107 statically prerendered docs component pages, each carrying a rendered preview. The typeset session's working tree had already removed those pages for the registry's move to its own repository, so committing the tree as it stood was the fix.

| | Job 274 | Job 275 |
|---|---|---|
| Build output | 274 MB | 162 MB |
| Prerendered app pages | 177 MB | 67 MB |
| Result | failed on size | live |

## What went into the commit

All 50 changed files:

- `/erd`: the policy database on a pan-zoom canvas, 187 relations in 15 domain groups with declared and inferred links, hide with undo and redo, snapshot script at `scripts/erd/snapshot.mjs`.
- The gate gates: no close cross on the scope card for anyone but an admin. `reader_profiles.admin`, set by hand, rides the session token; the rule opens everything for it and the card still draws with the cross. Migration in `scripts/sql/2026-09-14-admin.sql`.
- The registry's docs pages, helpers and data leave the app ahead of their own repository. The sidebar-09 block stays, since the workspace designer renders the Agent Inbox from it.
- The Amplify log prints per-route sizes under `.next/server/app`, so the next overrun can be read off the log rather than guessed at.

The two reference files at the repository root, the LegiScan price list and the ERD image, stayed untracked.

## Verified on production

- Home, `/erd` and the workspace dashboard answer 200.
- Congress bills answer 200 to a stranger. New York bills answer 403.
- Build, deploy and verify steps all succeeded.

## Left over

The dev box's checkout is still on an older commit, carrying the same content as uncommitted edits. A pull there at the next dev-server restart tidies it.
