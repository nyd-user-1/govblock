// /watches/new wears the records shell (Brendan, 2026-09-20): the left rail
// as a sheet, the page beside it. In a group so the shell wraps the create
// flow alone — the watches list, a watch and the inbox are the dashboard's
// kind of page, and keep their own frame until they are given it.
export { default } from "@/app/docs/layout"
