// /calendar is /workspace/calendar's calendar since 2026-09-21 (Brendan), drawn
// by the page; the shell that stood here, with the month in the site rail, went
// with the calendar it framed.
export default function CalendarLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
