// The hub and its charts wear the records shell (Brendan, 2026-09-20), as
// /state does: the left rail, the center column and the page's right rail. In
// a group so the shell wraps those two pages alone — charts/embed, outside
// the group, is a bare chart for an iframe and wears nothing.
export { default } from "@/app/docs/layout"
