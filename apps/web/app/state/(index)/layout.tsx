// /state wears the records shell (Brendan, 2026-09-18): the left rail, the
// center column and the page's right rail. In a group of its own so the shell
// wraps the index alone — the hubs under /state/[state] draw RailsFrame
// themselves, and a layout one level up would nest the two.
export { default } from "@/app/docs/layout"
