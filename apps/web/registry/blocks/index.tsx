import Sidebar07 from "@/registry/blocks/sidebar-07/page"

// The blocks on file, by registry name — what /view/<style>/<name> renders.
export const blockComponents: Record<string, React.ComponentType> = {
  "sidebar-07": Sidebar07,
}
