# Simulator handoff, 2026-09-17 13:40

Read this first, then `docs/design/simulator/` and the memory file `simulator-design.md` if this account can see it.

## Where things stand
- Branch `feature/typeset-flip`. Committed: `022347a` (the simulator, its map, loaders, SQL, prototypes) and `d610371` (the page laid out as the record pages: header block, Summary, then Chart, Files, Case study as h3 sub-sections; ranked-choice block; buttons; Sessions menu).
- Uncommitted, all typechecked and compiling: `apps/web/components/elections/{simulator,chamber,ranked-choice}.tsx` — the header facts line no longer says "Primaries on file"; the ranked-choice reset chip reads "All"; a chamber opened from the Files table starts on All; Open (T2)/(T4) buttons are hidden, not greyed, where a chamber's primary results are not on file; the Chambers table lists only chamber-years with primary results on file (87 rows, 45 states, 201 chamber-years: House 2012–2022, states 2014/2016/2018). Commit these first.
- Brendan reviews on the local dev server, http://localhost:3000/simulator (a `next dev` on the Mac, running for a day). The EC2 dev box is off; ignore it unless he asks.
- Nothing is pushed. Never push without his word.

## The page, as agreed
Header block (flag 90×60, name, facts line "General election 2022", Copy page with the word and a menu holding "Page tour" and "Map", prev/next arrows), a rule, then in the 640px record column: h2 Summary (the claim, names as chips, figures as `<code>`), h3 Chart (a PreviewFrame box: rule buttons left — All, Closed, Open (T2), Open (T4) — Sessions menu right, the ring, the counts row with the General/Primary legend), h3 Files (three animated tabs over a 300px table, no search), h3 Case study (five cards, three then two, open and hovered in `bg-muted`). Right rail carries the table of contents. Spacing comes only from the typeset stylesheet; do not add margins.

Ranked-choice block: candidate chips, the vote bar edge to edge with "Round n" over each bar's left end (only when more than one round), the majority tick with no word, no text outside the bar, the one-on-one table under it with no header row, lead sentence or caption.

## Rules Brendan set today (do not relitigate)
- Claim first, then the illustration. No chart without a sentence that says what it tests.
- Match his screenshots exactly; his devtools edits are not on disk (`simulator.html` at the repo root is one such copy — diff before assuming).
- Only chambers with primary results on file belong in the Files table; a chamber without them can only show All and Closed.
- No swing, no votes-against-seats, no funnel, no "your vote is worth" on this page. The map console keeps the swing and matched-to-votes rules.
- Say "primary results not on file", never "open rules missing".
- One tool call at a time; no subagents; bounded typecheck only: `cd apps/web && node --max-old-space-size=2048 ../../scripts/check-files.cjs <files>`. Never run the whole-project TypeScript compiler, and never put the name of the JavaScript linter in a shell command: a hook blocks both.

## Open items, in order
1. Commit the uncommitted changes above.
2. S3 cache headers: objects under `elections/` in `govblock-geo-638175140432` carry no Cache-Control, so every reload refetches the ballots/results/primaries. Set `max-age=86400` on the prefix and add `--cache-control` to the loaders' uploads in `scripts/elections/load.mjs` and `map.mjs`. Brendan said yes in principle; confirm before running the bucket command.
3. The tour's steps still mention "cases"; retarget to `[data-tour="cases"]` under Case study and reword.
4. `/simulator/map` has not had the layout pass; it keeps the console frame like /map.
5. Loader bugs found: House winners with quotation marks in their names carry a blank party (8 in 2024); Hank Johnson's 2022 top_two splits on the quotes; ranked-choice ballot files carry no party (Alaska and Maine are hard-coded in `candidateColors`).
6. Not loaded: 2024 House primaries (FEC hasn't published) and any state legislative primaries after 2018.
